<p id="notice"><%= notice %></p>

<h1>Carrots</h1>


 <!-- HERE COMES MY GRID LINE TO REPEAT -->
<% @products.each do |product| %>
  <div class="gd-container">
    <div class="item perso">
      <div class="sub-item">
        <%= image_tag product.profile_pic %>
      </div>
      <div class="sub-item">
        <p class="name"><%= product.owner_name %> <span class="distance"><i class="fas fa-map-marker-alt"></i> <%= product.distance %></span></p>
        <p><%= product.description %></p>
      </div>
    </div>
    <div class="item buy">
        <a href="" data-toggle="modal" data-target="#myModal">
            <i class="fas fa-shopping-basket fit-sz"></i>
        </a>
    </div>
    <div class="item big-price"><%= product.price_corrected %></div> 
    <div class="item price"><%= product.price_theoric %></div>
    <div class="item"><i class="fas fa-road"></i> <%= product.impact %></div>
  </div>
  <div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog modal-lg">
  <!-- Modal content-->
      <div class="modal-content">
        <div class="modal-header">
          <%= image_tag product.profile_pic %>
          <h4 class="modal-title"><%= product.owner_name %>
            <br> vous remercie chaleureusement.
          </h4>
          <button type="button" class="close" data-dismiss="modal">&times;</button>
        </div>
        <div class="modal-body text-center">
          <p>
            <i class="fas fa-leaf"></i>
          </p>
          <p>
            <i class="fas fa-money-bill-alt"></i>
          </p>
          <p>Some text in the modal.</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>
<% end %>


<!-- END OF THE GRID LINE -->
<br>

<%= link_to 'New Product', new_product_path %>
